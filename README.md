<!-- Root README: describes how to run the combined client + server workspace. -->
# Szabina Therapy Monorepo

This repository hosts a React client and an Express/Mongoose API that will evolve into the Szabina Therapy platform. Install dependencies at the root (which installs both workspaces) and run `npm run dev` to launch both apps in parallel.
