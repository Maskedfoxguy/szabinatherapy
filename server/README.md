

## Getting started
1. Copy `.env.example` to `.env` and fill in the MongoDB connection string along with admin seed credentials.
2. Run `npm install` inside `server/` to install API dependencies (or from the repo root to install every workspace).
3. Execute `npm run seed` to ensure an initial admin exists, then `npm run dev` to start the API with hot reload.
