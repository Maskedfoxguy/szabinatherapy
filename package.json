{
  "_comment": "Root package: manages both workspaces and the shared dev script.",
  "name": "szabinatherapy",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for the Szabina Therapy learning project (client + server)",
  "scripts": {
    "dev": "concurrently \"npm --prefix client start\" \"npm --prefix server dev\""
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
